/**
 * Components Index
 * 组件规范统一导出
 */

import buttonTokens from './button';
import inputTokens from './input';
import cardTokens from './card';

export { buttonTokens, inputTokens, cardTokens };

export default {
  button: buttonTokens,
  input: inputTokens,
  card: cardTokens,
};
